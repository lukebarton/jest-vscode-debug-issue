const example = require("../index");

describe("example", () => {
  it("should do proper breakpoints", async () => {
    expect(await example("a")).toEqual("A");
    expect(await example("b")).toEqual("B");
    expect(await example("c")).toEqual(":)");
    expect(await example(":(")).toEqual(":)");
  });
});

describe("example", () => {
  it("should do proper breakpoints", async () => {
    expect(await example("a")).toEqual("A");
    expect(await example("b")).toEqual("B");
    expect(await example("c")).toEqual(":)");
    expect(await example(":(")).toEqual(":)");
  });
});

describe("example", () => {
  it("should do proper breakpoints", async () => {
    expect(await example("a")).toEqual("A");
    expect(await example("b")).toEqual("B");
    expect(await example("c")).toEqual(":)");
    expect(await example(":(")).toEqual(":)");
  });
});

describe("example", () => {
  it("should do proper breakpoints", async () => {
    expect(await example("a")).toEqual("A");
    expect(await example("b")).toEqual("B");
    expect(await example("c")).toEqual(":)");
    expect(await example(":(")).toEqual(":)");
  });
});

describe("example", () => {
  it("should do proper breakpoints", async () => {
    expect(await example("a")).toEqual("A");
    expect(await example("b")).toEqual("B");
    expect(await example("c")).toEqual(":)");
    expect(await example(":(")).toEqual(":)");
  });
});

describe("example", () => {
  it("should do proper breakpoints", async () => {
    expect(await example("a")).toEqual("A");
    expect(await example("b")).toEqual("B");
    expect(await example("c")).toEqual(":)");
    expect(await example(":(")).toEqual(":)");
  });
});

describe("example", () => {
  it("should do proper breakpoints", async () => {
    expect(await example("a")).toEqual("A");
    expect(await example("b")).toEqual("B");
    expect(await example("c")).toEqual(":)");
    expect(await example(":(")).toEqual(":)");
  });
});

describe("example", () => {
  it("should do proper breakpoints", async () => {
    expect(await example("a")).toEqual("A");
    expect(await example("b")).toEqual("B");
    expect(await example("c")).toEqual(":)");
    expect(await example(":(")).toEqual(":)");
  });
});
